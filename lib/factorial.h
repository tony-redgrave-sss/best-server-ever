//
// Created by dante on 4/11/23.
//

#ifndef BEST_SERVER_EVER_FACTORIAL_H
#define BEST_SERVER_EVER_FACTORIAL_H

#include <stdint.h>

/**This function calculates the factorial of its request iteratively,
 * or 20! if n > 20.
 */
uint64_t factorial(uint64_t request);
uint64_t min(uint64_t m, uint64_t n);

#endif //BEST_SERVER_EVER_FACTORIAL_H
